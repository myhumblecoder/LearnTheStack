DATABASE_URL="postgresql://learnthestack:learnthestack@localhost:5432/learnthestack"

# AI tutor model selection:
# - If ANTHROPIC_API_KEY is set, the tutor uses Anthropic Claude Haiku 4.5
#   (hosted, deploys to Vercel). Set this in production.
# - Otherwise it falls back to local Ollama (free offline dev).
ANTHROPIC_API_KEY=""
OLLAMA_MODEL="qwen2.5:7b"
